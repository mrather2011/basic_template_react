import React from "react";
import classes from "./Hero.module.scss";

const Hero = (props) => {
  return (
    <div className="Container">
      <h1>HERO</h1>
    </div>
  );
};

export default Hero;
